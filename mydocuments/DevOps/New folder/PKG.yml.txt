[root@master ansible]# cat pkg.yml
---
- hosts: myservers
  user: ansible
  become: yes
  become_method: sudo
  tasks:
  - name: 1. Deploy FTP
    yum: name=vsftpd state=present
  - name: 2. Enable and start FTP Service
    service: name=vsftpd state=started enabled=yes

